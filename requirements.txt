# 作业批改系统依赖（Python 3.9）
#
# 关键约束：paddlepaddle 2.6.2 针对 numpy 1.x 编译，
# 若使用 numpy>=2.0 会出现 ABI 不兼容错误，务必锁定 numpy==1.26.4。
numpy==1.26.4
paddlepaddle==2.6.2
paddleocr==2.7.0.3
Flask==3.1.3
flask-cors==6.0.5
openai==2.48.0
python-dotenv==1.2.1

# 向量嵌入（离线语义匹配）。torch 锁定 2.5.1：2.8.0 的 shm.dll 在本机报 WinError 127
torch==2.5.1
sentence-transformers==5.1.2

# 测试依赖
pytest==8.4.2
