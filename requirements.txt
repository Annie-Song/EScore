# 作业批改系统依赖（Python 3.9）
#
# 关键约束：paddlepaddle 2.6.2 针对 numpy 1.x 编译，
# 若使用 numpy>=2.0 会出现 ABI 不兼容错误，务必锁定 numpy==1.26.4。
numpy==1.26.4
paddlepaddle==2.6.2
paddleocr==2.7.0.3
Flask==3.1.3
flask-cors==6.0.5
openai==2.48.0
python-dotenv==1.2.1
# 支付宝沙箱网关（可选）：SDK 未安装时支付流程回退本地演示网关，升级仍可走通
python-alipay-sdk==3.3.0

# 向量嵌入（离线语义匹配）。torch 锁定 2.5.1：2.8.0 的 shm.dll 在本机报 WinError 127
torch==2.5.1
sentence-transformers==5.1.2
# 离线加载用 huggingface_hub.snapshot_download 解析本地快照（sentence-transformers 的传递依赖，显式锁定）
huggingface-hub==0.36.2

# 报告下载：生成 Word 版批改报告（.docx）
python-docx==1.2.0

# A8 向量嵌入微服务：独立 FastAPI 进程
fastapi==0.128.8
uvicorn==0.39.0
# 嵌入服务 HTTP 客户端直接依赖
httpx==0.28.1

# 测试依赖
pytest==8.4.2

# 生产 WSGI 服务器（仅容器内 gunicorn 入口使用；gunicorn 不支持 Windows，原生开发仍走 run.py，install 无副作用）
gunicorn==23.0.0
